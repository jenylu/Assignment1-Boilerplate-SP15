<h1 class="par5">Instagram Feed</h1>
<div class="container-fluid">
	<div class="row">
	    {{#each photos}}
	    <div class="col-sm-3">
	    	<div class="insta">
		      <img src="{{url}}" class="img-responsive" class="img hashtag-img">
		      <p>{{caption}}</p>
	  		</div>
	      </div>
	    {{/each}}  
	</div>
</div>
